# Trained model output

`training/train.py` writes `model.json`, a `.bin` weight shard, and
`metadata.json` here after training. No model is included yet because the
repository does not contain a labelled 23-class image dataset.
